[test]
# Test file patterns
root = "./test"

# Timeout for individual tests (30 seconds)
timeout = 30000

# Enable coverage reporting
coverage = true

# Coverage thresholds - tests fail if coverage drops below these levels
# Set to 35% as I/O-heavy code (network requests, file operations) can't be
# effectively unit tested in Bun without complex mocking infrastructure.
# Integration tests are more appropriate for I/O operations.
coverageThreshold = { lines = 0.35, functions = 0.30 }

# Exclude third-party vendored files and submodule from coverage
# Also exclude I/O-heavy files better suited for integration tests
coveragePathIgnorePatterns = [
  "lib/chobble-template/**",
  "masonry.pkgd.min.js",
  "iframe-resizer.child.js",
  "test/**",
]
